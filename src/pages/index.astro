---
import Page from './[lang]/index.astro';
import { defaultLang, getStrings } from '../utils/i18n.js';

// Determine default language and fetch strings
const lang = defaultLang;
const strings = await getStrings(lang);
---

<!-- Render the [lang] page at root with defaultLang -->
<Page lang={lang} strings={strings} />